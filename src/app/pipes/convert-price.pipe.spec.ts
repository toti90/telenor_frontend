import { ConvertPricePipe } from './convert-price.pipe';

describe('ConvertPricePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertPricePipe();
    expect(pipe).toBeTruthy();
  });
});
